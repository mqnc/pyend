for ignoring indentation, the tokenizer must not hickup.
so we have to modify the readline function to strip all whitespaces in the beginning of a line if the last returned token was a NEWLINE or NL
(tokenizer must not be in a string)